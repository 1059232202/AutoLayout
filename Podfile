# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AutoLayout自动布局' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for AutoLayout自动布局
  pod 'Masonry'
  
  target 'AutoLayout自动布局Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AutoLayout自动布局UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

# 1.lilin:AutoLayout自动布局 lilin$ pod init
# 2.加入要管理的库如 pod 'Masonry'
# 3.lilin:AutoLayout自动布局 lilin$ pod install --verbose --no-repo-update
# 4.库下载完毕。去项目所在目录点击.xcworkspace打开项目。
